import { StyleHeader } from "./Styles/Header.styled";
import { Button } from "./Styles/Button.styled";
export default function Header() {
  return (
    <StyleHeader>
      <Button>Lets try if it works</Button>
    </StyleHeader>
  );
}
